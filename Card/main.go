package main

func main() {
	// cards := deck{newCard(), "Airman"}

	// cards := newDeck()

	// fmt.Println(deal(cards, 5))

	//*return ค่าออกมา 2 ค่า สามารถเก็บค่าที่ return ไว้ที่ตัวแปรได้
	// hand, remainingCards := deal(cards, 5)

	// hand.print() //*return ค่าาที่ 1

	// fmt.Println(("----------------"))

	// remainingCards.print() //*return ค่าที่ 2
	// greeting := "Hi there!"
	// fmt.Println([]byte(greeting)) //*เปลี่ยนประเภทเป็นแบบ byte รหัส ascii

	// cards := newDeck() //*นำข้อมูล retrurn เก็บไว่ใน cards
	// fmt.Println(cards.toString())
	// cards.savetoFile("My_cards") //*save ข้อมูล card ลง hard drive

	// cards := newDeckFromFile("Myrds")
	// cards.print()

	// cards := newDeck()
	// cards.shuffle()
	// cards.print()

	//* Assignment even_odd
	// even_odd()

}

// func newCard() string { //* ประกาศการ return ค่า แบบ string
// return "Five of Diamonds"
// }
